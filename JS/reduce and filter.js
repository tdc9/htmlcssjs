const numbers = [1,2,3,4,5,6];
const evennumbers.filter((item) => {item %2==0});
cons;ole.log(even)

const marks = [
    { subject: "English", marks: 75},
    { subject: "Hindi", marks: 80},
        { subject: "Maths", marks: 65},
    ];
    
    
const result = marks.reduce(function (totalMarks = 0, subj){
    
    console.log(subj);
    
    return +totalMarks + +subj.marks;
    
    });
    
    console.log(result);